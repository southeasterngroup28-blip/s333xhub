// Sentences that appear on more than one screen live here once, so the
// same idea never ends up phrased five ways.

/** After a report is filed, in feed, comments and chat alike. */
export const REPORT_SENT = 'Reported. The artist reviews reports within 24 hours.';
export const REPORT_FAILED = 'Could not send the report.';

/** The error card's pill. Nothing else says RETRY. */
export const RETRY = 'Try again';
/** The error card's one sub line. The pill already says what to do. */
export const OFFLINE_SUB = 'Check your connection.';
